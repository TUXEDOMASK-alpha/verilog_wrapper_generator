# Instance to Top Port Mappings with intentional errors
# Format: instance_name.port_name -> top_port_name

[INSTANCE_TO_TOP]
# Valid connection
good_inst.a -> sys_a
good_inst.b -> sys_b
good_inst.sum -> sys_sum

# Invalid connections for testing
good_inst.nonexistent_port -> sys_a
bad_file_inst.a -> sys_a

# Trying to tie an output port (should be error)
good_inst.sum -> TIE0